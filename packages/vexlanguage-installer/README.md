# vexlanguage-installer

> npm-based installer for **VexLanguage** native binaries.  
> Node is **only used for installation**. Running `vex` executes the **native** Vex binary (no Node runtime).

## Usage
```bash
npm i -g vexlanguage-installer
vex --version
```
